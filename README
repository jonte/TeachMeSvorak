This is basically a timer changing keyboard layouts using setxkbmap. The app is only tested on Linux (Ubuntu 10.10), and uses the python libnotify bindings, pygtk and the setxkbmap.

Launch the application by running main.py, tested with python 2.6
